# Contributing

Fork the repository, create a focused feature branch, and open a pull request against `master`. Run `npm ci`, `npx expo-doctor`, and `npx expo export:web` before submitting. Pull requests must pass automated checks, receive code-owner approval, and resolve all review threads. Maintainers squash-merge approved changes.
